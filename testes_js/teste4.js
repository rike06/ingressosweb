let limite = 100;
let saldo_atual = 20;

let resultado = (saldo_atual < limite) ? "Necessito depositar $" : "Não necessito Depósito";
console.log(r)

/*

sintaxe:
let variável = (condição) ? //instrução se true : //instrução se falso
*/
